# Spam-Detection-System
A full-stack spam classifier using a Multinomial Naive Bayes model from scikit-learn. The system includes a Node.js (Express) API for handling requests and a frontend built with HTML, CSS, and JavaScript for real-time predictions.
